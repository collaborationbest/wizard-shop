/** @type {import("prettier").Config} */
const config = {
    trailingComma: 'all',
    singleQuote: true,
    jsxSingleQuote: true,
    printWidth: 80,
    tabWidth: 4,
    useTabs: false,
    bracketSpacing: true,
    bracketSameLine: false,
    arrowParens: 'avoid',
    endOfLine: 'auto',
    plugins: [],
};

export default config;
