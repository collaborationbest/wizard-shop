import { Text } from '@/components/ui';

import styles from './head.module.scss';

export const Head = () => {
    return (
        <div className={styles.head}>
            <Text size='lg'>КОЛЕСО ФОРТУНЫ</Text>
        </div>
    );
};
